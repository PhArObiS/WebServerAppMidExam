{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  // Connection string for SQL Server database.
  "ConnectionStrings": {
    // Change the value of "Server" to the name of your SQL Server instance and the value of "Database" to the name of your database.
    "DefaultConnection": "Server=localhost\\SQLEXPRESS;Database=MidTermDb;Trusted_Connection=True;TrustServerCertificate=True"
  }
}
